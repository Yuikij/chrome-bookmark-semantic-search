<!DOCTYPE html>
<html data-theme="dark">

<head>
    <meta charset="utf-8">
    <title>智能书签控制面板</title>
    <style>
        /* CSS Variables for unifying light/dark themes */
        :root {
            --bg-main: #f4f7f6;
            --bg-surface: #ffffff;
            --bg-surface-hover: #f8fafc;
            --bg-active: #eff6ff;
            --text-main: #1d1d1f;
            --text-sec: #64748b;
            --text-muted: #94a3b8;
            --border-color: #e2e8f0;
            --accent: #3b82f6;
            --accent-hover: #2563eb;
            --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
            --shadow-md: 0 4px 20px rgba(0, 0, 0, 0.05);
            --shadow-lg: 0 10px 40px rgba(0, 0, 0, 0.1);
            --glass-bg: rgba(255, 255, 255, 0.85);

            --info-bg: #eff6ff;
            --info-text: #1e3a8a;
            --success-bg: #ecfdf5;
            --success-border: #a7f3d0;
            --success-text: #065f46;
            --warning-bg: #fffbeb;
            --warning-text: #b45309;
            --danger-btn: #ef4444;
        }

        [data-theme="dark"] {
            --bg-main: #0d1117;
            --bg-surface: #161b22;
            --bg-surface-hover: #21262d;
            --bg-active: rgba(59, 130, 246, 0.15);
            --text-main: #c9d1d9;
            --text-sec: #8b949e;
            --text-muted: #484f58;
            --border-color: #30363d;
            --accent: #58a6ff;
            --accent-hover: #79c0ff;
            --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
            --shadow-md: 0 4px 20px rgba(0, 0, 0, 0.4);
            --shadow-lg: 0 10px 40px rgba(0, 0, 0, 0.6);
            --glass-bg: rgba(22, 27, 34, 0.85);

            --info-bg: #1f2937;
            --info-text: #60a5fa;
            --success-bg: rgba(16, 185, 129, 0.1);
            --success-border: rgba(16, 185, 129, 0.2);
            --success-text: #34d399;
            --warning-bg: rgba(245, 158, 11, 0.1);
            --warning-text: #fbbf24;
            --danger-btn: #dc2626;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Inter', 'Segoe UI', Roboto, sans-serif;
            background: var(--bg-main);
            color: var(--text-main);
            padding: 30px;
            margin: 0;
            transition: background 0.3s, color 0.3s;
        }

        .container {
            max-width: 1500px;
            width: 98%;
            margin: 0 auto;
            background: var(--bg-surface);
            padding: 30px;
            border-radius: 12px;
            box-shadow: var(--shadow-md);
            border: 1px solid var(--border-color);
        }

        h1 {
            margin-top: 0;
            color: var(--text-main);
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 15px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            font-size: 22px;
            font-weight: 600;
        }

        .stats {
            display: flex;
            gap: 15px;
            margin-bottom: 25px;
        }

        .stat-box {
            flex: 1;
            background: var(--bg-surface-hover);
            padding: 16px;
            border-radius: 10px;
            text-align: center;
            border: 1px solid var(--border-color);
            transition: transform 0.2s;
        }

        .stat-box:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-sm);
        }

        .stat-box h3 {
            margin: 0 0 8px;
            font-size: 13px;
            color: var(--text-sec);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .stat-box .num {
            font-size: 26px;
            font-weight: 700;
            color: var(--accent);
        }

        .tabs {
            display: flex;
            gap: 8px;
            margin-bottom: 25px;
            border-bottom: 1px solid var(--border-color);
            padding-bottom: 12px;
        }

        .tab-btn {
            background: transparent;
            border: none;
            padding: 10px 18px;
            font-size: 15px;
            font-weight: 600;
            color: var(--text-sec);
            cursor: pointer;
            border-radius: 8px;
            transition: 0.2s;
        }

        .tab-btn:hover {
            background: var(--bg-surface-hover);
            color: var(--text-main);
        }

        .tab-btn.active {
            background: var(--bg-active);
            color: var(--accent);
        }

        .view-section {
            display: none;
            animation: fadeIn 0.3s ease;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(5px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .view-section.active {
            display: block;
        }

        .index-view {
            border: 1px solid var(--border-color);
            border-radius: 10px;
            height: 620px;
            overflow-y: auto;
            background: var(--bg-surface);
        }

        /* Scrollbar tweaks */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }

        ::-webkit-scrollbar-thumb {
            background: var(--border-color);
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: var(--text-muted);
        }

        /* Folder structure */
        .folder-item {
            border-bottom: 1px solid var(--border-color);
        }

        .folder-item:last-child {
            border-bottom: none;
        }

        .folder-title {
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: var(--bg-surface-hover);
            padding: 14px 20px;
            cursor: pointer;
            transition: 0.2s;
            position: sticky;
            top: 0;
            z-index: 10;
            border-left: 3px solid transparent;
        }

        .folder-title:hover {
            background: var(--bg-active);
            border-left-color: var(--accent);
        }

        .folder-status {
            font-size: 12px;
            background: var(--bg-surface);
            border: 1px solid var(--border-color);
            padding: 2px 8px;
            border-radius: 12px;
            color: var(--text-sec);
        }

        .folder-content {
            background: var(--bg-surface);
            display: none;
        }

        .folder-content.open {
            display: block;
        }

        .bm-row {
            padding: 12px 20px;
            border-bottom: 1px dashed var(--border-color);
            font-size: 14px;
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .bm-row:last-child {
            border-bottom: none;
        }

        .bm-title {
            color: var(--text-main);
            font-weight: 500;
            text-decoration: none;
            transition: color 0.2s;
        }

        .bm-title:hover {
            color: var(--accent);
        }

        .bm-url {
            color: var(--text-muted);
            font-size: 12px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        /* Twillot-like Split View Logic */
        .tw-split-layout {
            display: flex;
            height: 620px;
            border: 1px solid var(--border-color);
            border-radius: 10px;
            overflow: hidden;
            background: var(--bg-surface);
        }

        .tw-list-pane {
            flex: 1;
            max-width: 480px;
            min-width: 320px;
            border-right: 1px solid var(--border-color);
            overflow-y: auto;
            background: var(--bg-surface-hover);
            display: flex;
            flex-direction: column;
        }

        .tw-detail-pane {
            flex: 2;
            overflow-y: auto;
            background: var(--bg-surface);
            position: relative;
        }

        .tw-list-item {
            padding: 15px 20px;
            border-bottom: 1px solid var(--border-color);
            cursor: pointer;
            display: flex;
            gap: 12px;
            transition: 0.2s;
            background: transparent;
        }

        .tw-list-item:hover {
            background: var(--bg-surface);
        }

        .tw-list-item.selected {
            background: var(--bg-surface);
            border-left: 3px solid var(--accent);
        }

        .list-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--border-color);
            object-fit: cover;
            flex-shrink: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-sec);
            font-weight: bold;
        }

        .list-content {
            flex: 1;
            min-width: 0;
        }

        .list-header {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            margin-bottom: 4px;
        }

        .list-name {
            font-weight: 600;
            font-size: 14px;
            color: var(--text-main);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .list-handle {
            font-size: 12px;
            color: var(--text-muted);
        }

        .list-snippet {
            font-size: 13px;
            color: var(--text-sec);
            display: -webkit-box;
            -webkit-line-clamp: 2;
            line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            line-height: 1.4;
        }

        .list-folder-badge {
            display: inline-block;
            font-size: 11px;
            padding: 2px 6px;
            border-radius: 4px;
            margin-top: 8px;
            background: var(--bg-active);
            color: var(--accent);
            border: 1px solid var(--border-color);
        }

        .detail-empty {
            position: absolute;
            inset: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            color: var(--text-muted);
            font-size: 15px;
        }

        .detail-container {
            padding: 30px;
            max-width: 700px;
            margin: 0 auto;
        }

        .detail-author-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .detail-author-left {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .detail-avatar {
            width: 52px;
            height: 52px;
            border-radius: 50%;
            background: var(--border-color);
            object-fit: cover;
        }

        .detail-name-row {
            display: flex;
            flex-direction: column;
        }

        .detail-name {
            font-size: 18px;
            font-weight: 700;
            color: var(--text-main);
        }

        .detail-handle {
            font-size: 14px;
            color: var(--text-sec);
            margin-top: 2px;
        }

        .detail-text {
            font-size: 16px;
            line-height: 1.6;
            color: var(--text-main);
            margin-bottom: 25px;
            white-space: pre-wrap;
            word-break: break-word;
        }

        .detail-media {
            margin-bottom: 20px;
            text-align: center;
        }

        .detail-media img {
            max-width: 100%;
            max-height: 450px;
            border-radius: 12px;
            border: 1px solid var(--border-color);
            cursor: pointer;
            transition: transform 0.2s;
            object-fit: contain;
        }

        .detail-media img:hover {
            transform: scale(1.01);
        }

        .detail-stats {
            display: flex;
            gap: 25px;
            padding: 15px 0;
            border-top: 1px solid var(--border-color);
            border-bottom: 1px solid var(--border-color);
            margin-top: 20px;
            color: var(--text-sec);
            font-size: 14px;
        }

        .detail-actions {
            margin-top: 20px;
            display: flex;
            gap: 10px;
        }

        .btn {
            padding: 8px 16px;
            background: var(--bg-surface);
            color: var(--text-main);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            cursor: pointer;
            font-size: 13px;
            font-weight: 500;
            transition: 0.2s;
            display: inline-flex;
            align-items: center;
            gap: 6px;
        }

        .btn:hover {
            background: var(--bg-surface-hover);
        }

        .btn-primary {
            background: var(--accent);
            border-color: var(--accent);
            color: white;
        }

        .btn-primary:hover {
            background: var(--accent-hover);
            color: white;
        }

        .btn-danger {
            background: var(--danger-btn);
            border-color: var(--danger-btn);
            color: white;
        }

        .btn-danger:hover {
            opacity: 0.9;
            color: white;
        }

        .btn-success {
            background: var(--success-bg);
            border-color: var(--success-border);
            color: var(--success-text);
        }

        .btn-success:hover {
            filter: brightness(0.95);
        }

        .btn-icon {
            padding: 8px;
            border-radius: 50%;
            width: 36px;
            height: 36px;
            justify-content: center;
            font-size: 16px;
        }

        .status-banner {
            padding: 14px 20px;
            border-radius: 8px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 14px;
        }

        .status-success {
            background: var(--success-bg);
            border: 1px solid var(--success-border);
            color: var(--success-text);
        }

        .status-info {
            background: var(--info-bg);
            border: 1px solid var(--border-color);
            color: var(--info-text);
        }

        .status-warning {
            background: var(--warning-bg);
            border: 1px solid var(--warning-bg);
            color: var(--warning-text);
        }

        .glass-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.4);
            z-index: 9999;
            justify-content: center;
            align-items: center;
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            transition: opacity 0.3s ease;
        }

        .glass-dialog {
            background: var(--glass-bg);
            padding: 24px;
            border-radius: 16px;
            width: 420px;
            box-shadow: var(--shadow-lg);
            border: 1px solid var(--border-color);
            backdrop-filter: blur(20px) saturate(180%);
            -webkit-backdrop-filter: blur(20px) saturate(180%);
            color: var(--text-main);
        }

        .glass-dialog-title {
            margin-top: 0;
            color: var(--text-main);
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 18px;
            font-weight: 600;
        }

        .glass-dialog-msg {
            color: var(--text-sec);
            font-size: 15px;
            line-height: 1.6;
            margin-bottom: 20px;
            white-space: pre-wrap;
        }

        .glass-input {
            width: 100%;
            box-sizing: border-box;
            padding: 12px;
            border: 1px solid var(--border-color);
            border-radius: 10px;
            margin-bottom: 20px;
            font-size: 14px;
            outline: none;
            background: var(--bg-main);
            color: var(--text-main);
            transition: border 0.2s, background 0.2s;
        }

        .glass-input:focus {
            border-color: var(--accent);
            background: var(--bg-surface);
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.15);
        }

        .glass-btn-group {
            display: flex;
            justify-content: flex-end;
            gap: 10px;
        }

        .glass-btn {
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            border: none;
            transition: 0.2s;
        }

        .glass-btn-cancel {
            background: var(--bg-surface-hover);
            border: 1px solid var(--border-color);
            color: var(--text-main);
        }

        .glass-btn-cancel:hover {
            background: var(--border-color);
        }

        .glass-btn-confirm {
            background: var(--accent);
            color: white;
            box-shadow: 0 2px 8px rgba(59, 130, 246, 0.3);
        }

        .glass-btn-confirm:hover {
            background: var(--accent-hover);
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>
            <span>🌍 智能书签数据库</span>
            <div style="display: flex; gap: 10px; align-items: center;">
                <button class="btn btn-icon" id="themeToggleBtn" title="切换暗色/亮色模式">🌙</button>
                <button class="btn" id="refreshBtn">🔄 刷新数据</button>
                <button class="btn btn-primary" id="openTwitterBtn">🐦 同步新的推特书签</button>
                <button class="btn btn-danger" id="forceReinitBtn" title="清除模型缓存并重新下载配置新的特征提取模型">⚠️ 强制重置</button>
            </div>
        </h1>

        <div class="stats">
            <div class="stat-box">
                <h3>已纳管书签总数</h3>
                <div class="num" id="totalBookmarks">-</div>
            </div>
            <div class="stat-box">
                <h3>X / 推特 书签数</h3>
                <div class="num" id="totalXBookmarks" style="color: var(--accent);">-</div>
            </div>
            <div class="stat-box">
                <h3>识别出的分类夹</h3>
                <div class="num" id="totalFolders">-</div>
            </div>
            <div class="stat-box">
                <h3>引擎状态</h3>
                <div class="num" id="engineStatus" style="font-size: 16px; line-height: 38px;">检查中...</div>
            </div>
        </div>

        <div class="search-box" style="margin-bottom: 25px; display: flex; gap: 10px; align-items: center;">
            <input type="text" id="dashSearchInput" placeholder="🤖 试试用自然语言描述你想找的书签内容..."
                style="flex: 1; padding: 14px 20px; font-size: 16px; border: 2px solid var(--border-color); border-radius: 8px; outline: none; transition: 0.2s; background: var(--bg-surface); color: var(--text-main);"
                onfocus="this.style.borderColor='var(--accent)'" onblur="this.style.borderColor='var(--border-color)'">
            <button class="btn btn-primary" id="dashSearchBtn"
                style="padding: 14px 30px; font-size: 16px; display: flex; align-items: center; gap: 8px;">🔍
                语义搜索</button>
        </div>

        <div class="tabs">
            <button class="tab-btn active" data-target="tab-folders">📁 语义折叠树</button>
            <button class="tab-btn" data-target="tab-twitter">🐦 推特/X </button>
            <button class="tab-btn" data-target="tab-search" id="tabSearchBtn" style="display: none;">🔍 搜索结果</button>
        </div>

        <div id="tab-folders" class="view-section active">
            <div class="index-view" id="indexView"></div>
        </div>

        <div id="tab-twitter" class="view-section">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
                <p style="color: var(--text-sec); margin: 0; font-size: 14px;">专属推特/X 。左侧浏览列表，右侧查看详情并进行管理，体验大气舒畅。</p>
                <div style="display: flex; gap: 8px;">
                    <!-- Controls will be injected here if clustered -->
                    <button class="btn btn-success" id="categorizeTwitterBtn"
                        style="color: white; border-color: transparent;">🔮 基于大模型生成虚拟分类树</button>
                </div>
            </div>

            <div id="twitterCategorizeStatus" class="status-banner status-success" style="display: none;">
                <!-- Status innerHTML goes here -->
            </div>
            <div id="twActionControls" style="display: none; gap: 10px; margin-bottom: 15px;"></div>

            <!-- New Split View layout for Twillot style -->
            <div class="tw-split-layout" id="xViewSplit">
                <div class="tw-list-pane" id="xListPane">
                    <div style="padding: 40px; text-align: center; color: var(--text-sec);">
                        <div style="font-size: 30px; margin-bottom: 15px;">📊</div>
                        <div id="rawCountLabel">加载中...</div>
                        <div style="margin-top: 10px; font-size: 13px;">点击上方 <b>🔮 聚类按钮</b> 开始智能分类</div>
                    </div>
                </div>
                <div class="tw-detail-pane" id="xDetailPane">
                    <div class="detail-empty">
                        <div style="font-size: 40px; margin-bottom: 15px; opacity: 0.5;">👈</div>
                        <div>点击左侧列表查看详细推文内容</div>
                    </div>
                </div>
            </div>
        </div>

        <div id="tab-search" class="view-section">
            <div id="searchStatus" class="status-banner status-info" style="display: none;"></div>
            <div class="index-view" id="searchView"
                style="padding: 20px; display: flex; flex-direction: column; gap: 10px;"></div>
        </div>

        <div id="cDialogOverlay" class="glass-overlay">
            <div class="glass-dialog">
                <h3 id="cDialogTitle" class="glass-dialog-title">
                    <span style="font-size:20px;">ℹ️</span> 提示
                </h3>
                <div id="cDialogMessage" class="glass-dialog-msg"></div>
                <input type="text" id="cDialogInput" class="glass-input" style="display:none;">
                <div class="glass-btn-group">
                    <button id="cDialogCancel" class="glass-btn glass-btn-cancel">取消</button>
                    <button id="cDialogConfirm" class="glass-btn glass-btn-confirm">确定</button>
                </div>
            </div>
        </div>

        <!-- 图片预览大图弹窗 -->
        <div id="imagePreviewOverlay" class="glass-overlay"
            style="background:rgba(0,0,0,0.85); z-index:10000; flex-direction: column; backdrop-filter: blur(15px); -webkit-backdrop-filter: blur(15px);">
            <div id="imagePreviewClose"
                style="position:absolute; top:20px; right:30px; color:white; font-size:40px; cursor:pointer; user-select:none; width: 40px; height: 40px; display: flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(255,255,255,0.1); transition: 0.2s;">
                &times;</div>
            <img id="imagePreviewImg" src=""
                style="max-width:90%; max-height:85vh; border-radius:12px; box-shadow: var(--shadow-lg); object-fit:contain; border: 1px solid rgba(255,255,255,0.1);" />
            <a id="imagePreviewLink" href="" target="_blank"
                style="color:white; margin-top:20px; font-size:15px; font-weight: 500; background:rgba(255,255,255,0.15); padding:10px 24px; border-radius:30px; text-decoration:none; transition:background 0.2s; backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px); border: 1px solid rgba(255,255,255,0.2);">
                在新标签页中打开原图 ↗</a>
        </div>
    </div>
    <script src="dashboard.js"></script>
</body>

</html>